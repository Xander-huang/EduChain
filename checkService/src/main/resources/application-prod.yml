spring:
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: 6379
    timeout: 1000
    connect-timeout: 500
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1
        min-idle: 0
  servlet:
    multipart:
      max-file-size: 10MB # 最大文件
      max-request-size: 50MB # 最大请求
  mail:
    host:
    port:
    username:
    password:
    properties:
      mail:
        smtp:
          ssl:
            enable: true

server:
  port: 8084

grpc:
  host:
  serverPort: 8888

email:
  ip:
